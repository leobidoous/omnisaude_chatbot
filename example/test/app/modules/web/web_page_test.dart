import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:omnisaude_chatbot_example/app/modules/web/web_page.dart';

void main() {
  testWidgets('WebPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WebPage(title: 'Web')));
    //  final titleFinder = find.text('Web');
    //  expect(titleFinder, findsOneWidget);
  });
}
