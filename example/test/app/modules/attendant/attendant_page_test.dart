import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:omnisaude_chatbot_example/app/modules/attendant/attendant_page.dart';

void main() {
  testWidgets('AttendantPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AttendantPage(title: 'Attendant')));
    //  final titleFinder = find.text('Attendant');
    //  expect(titleFinder, findsOneWidget);
  });
}
