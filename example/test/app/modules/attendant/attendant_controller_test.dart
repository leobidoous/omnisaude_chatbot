import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:omnisaude_chatbot_example/app/modules/attendant/attendant_controller.dart';
import 'package:omnisaude_chatbot_example/app/modules/attendant/attendant_module.dart';

void main() {
  initModule(AttendantModule());
  // AttendantController attendant;
  //
  setUp(() {
    //     attendant = AttendantModule.to.get<AttendantController>();
  });

  group('AttendantController Test', () {
    //   test("First Test", () {
    //     expect(attendant, isInstanceOf<AttendantController>());
    //   });

    //   test("Set Value", () {
    //     expect(attendant.value, equals(0));
    //     attendant.increment();
    //     expect(attendant.value, equals(1));
    //   });
  });
}
