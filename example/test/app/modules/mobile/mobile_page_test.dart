import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:omnisaude_chatbot_example/app/modules/mobile/mobile_page.dart';

void main() {
  testWidgets('MobilePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MobilePage(title: 'Mobile')));
    //  final titleFinder = find.text('Mobile');
    //  expect(titleFinder, findsOneWidget);
  });
}
